[[en](./README.md)]

# Claws

Утилита для управления программируемым кейпадом: настройка профилей, назначение комбинаций на 16 кнопок и крестовину/стик, импорт/экспорт конфигураций и взаимодействие с устройством по последовательному порту.

## 📦 Описание
Claws — настольное приложение на Rust для пользователей программируемых кейпадов. Приложение помогает:
- просматривать и редактировать активный профиль кнопок и стика;
- назначать комбинации клавиш и специальные коды;
- управлять «мёртвой зоной» стика;
- импортировать/экспортировать профили в файл;
- записывать профиль во внутреннюю память (ОЗУ/ПЗУ) устройства;
- просматривать сведения об устройстве и версии прошивки;
- перезагружать устройство в загрузчик для обновления прошивки.

## 🛠 Технологии и стек
- **Язык/рантайм**: Rust 2024 Edition
- **GUI**: `iced` 0.13 (widgets, svg, image, tokio, advanced)
- **Асинхронность**: `tokio` 1.x
- **Последовательный порт**: `serialport` (с `serde`)
- **Конфигурации/профили**: `serde`, `confy`
- **Диалоги файлов**: `rfd`
- **Логирование**: `log`, `pretty_env_logger`
- **Ошибки**: `thiserror`, `anyhow`
- **Тема ОС**: `dark-light`
- **Nix**: flake с `crane`, `rust-overlay`, `flake-utils`, `git-hooks.nix`

Ссылки на ключевые файлы:
- Манифест: `Cargo.toml`
- Точка входа: `src/main.rs`
- UI и логика: `src/ui/mod.rs`, `src/ui/pages.rs`, `src/ui/styles/`
- Работа с устройством: `src/hardware/*` (buffers, commands, serial)
- Данные и конфиги: `src/data/*` (profiles, device, window, file_dialog)
- Ассеты: `assets/` (иконки, шрифты)
- Сборка через Nix: `flake.nix`, `nix/`

## 🚀 Установка и запуск
### Вариант 1: Cargo
Требуется установленный Rust (включая Cargo).

```bash
# Клонирование
git clone https://git.sr.ht/~fisuri/claws
cd claws

# Сборка релизной версии
cargo build --release

# Запуск
cargo run
```

### Вариант 2: Nix Flakes
Если у вас установлен Nix с включёнными flakes:
```bash
# Сборка пакета через flake
nix build
# Готовый бинарник будет в result/bin/
```
Конфигурация flake смотрит на `nix/default.nix` и использует `crane` для сборки Rust-проекта.

## 🗂 Структура проекта
```
claws/
  assets/             # Иконки и шрифты приложения
  src/
    assets.rs         # Константы приложения: имя, версия, шрифт, иконка
    main.rs           # Точка входа: запуск Iced-приложения и окна
    ui/               # UI, сообщения, страницы, стили
      mod.rs          # Сообщения, update/view, подписки, обработчики
      pages.rs        # Страницы
      styles/         # Стили
    data/             # Модели/конфиги
      profiles.rs     # Профиль: 16 кнопок × 6 кодов, настройки стика, I/O
      device.rs       # Информация об устройстве (версия, серийник и т.д.)
      ...
    hardware/         # Буферы и команды протокола, работа с Serial
      buffers/
      commands/
      serial/
    errors/           # Ошибки
    utils/            # Логгер и утилиты
  nix/                # Файлы сборки через Nix
  flake.nix           # Flake
  LICENSE             # Лицензия проекта
```

## ✨ Возможности
- **Профили**: редактирование активного профиля, смена названия
- **16 кнопок**: быстрое назначение до 6 кодов на кнопку и очищение
- **Стик**: настройка направлений и «мёртвой зоны»
- **ОЗУ/ПЗУ**: запись активного профиля в ОЗУ/ПЗУ ячейки устройства
- **Импорт/экспорт**: профиль в файл и из файла
- **Сведения об устройстве**: версия приложения и прошивки на экране «Обновление»
- **Подключение по Serial**: автоматический поиск порта, при недоступности — страница «Устройство не найдено»

## ❓ FAQ
- **Устройство не определяется приложением**
  Проверьте кабель/порт USB и доступ к последовательному порту. На Linux могут потребоваться udev-правила или добавление пользователя в группу, имеющую доступ к tty-устройствам — _dialout_.

- **Где хранятся профили на диске?**
  Профили сохраняются через `confy` в каталоге конфигурации ОС (создаётся подкаталог `profiles/`). Точный путь зависит от платформы — https://github.com/rust-cli/confy?tab=readme-ov-file#config-file-location.

- **Какие коды поддерживаются для кнопок/стика?**
  Поддерживаются стандартные коды клавиш (Esc, F1–F24, модификаторы и т.д.) и направления стика. Отображение см. в `src/data/profiles.rs` (`code_to_title`). Полный список — в коде проекта.

- **Поддерживаются ли темы (светлая/тёмная)?**
  Приложение использует `dark-light` для определения темы системы.

- **Есть ли автообновление прошивки?**
  Вкладка «Обновление» показывает версии, но процесс прошивки выполняется отдельно.

## 📄 License
Этот проект лицензирован в соответствии с [APACHE 2.0 License](./LICENSE).

## 👤 Автор
- **Yaroslav Malykh** — <fisuri@murena.io>

---

Дополнительно:
- Шрифты: используется Inter Medium (`assets/fonts/Inter-Medium.ttf`).
- Иконки: размещены в `assets/icons/` и встраиваются в бинарник.
- Под Windows при сборке в релизе скрывается консоль (`windows_subsystem = "windows"`).
- Логи включаются через `pretty_env_logger` и настраиваются переменной `RUST_LOG`.
